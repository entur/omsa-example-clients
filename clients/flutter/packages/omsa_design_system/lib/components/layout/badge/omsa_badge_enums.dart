enum OmsaBadgeVariant {
  primary,
  neutral,
  success,
  warning,
  negative,
  information,

  /// @deprecated use information instead
  info,

  /// @deprecated use negative instead
  danger,
}

enum OmsaBadgeType { status, bullet, notification }
