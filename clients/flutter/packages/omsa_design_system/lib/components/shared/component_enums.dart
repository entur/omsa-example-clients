/// Component mode for switching between default and contrast tokens
enum OmsaComponentMode {
  /// Standard mode for light backgrounds
  standard,

  /// Contrast mode for dark/colored backgrounds
  contrast,
}
